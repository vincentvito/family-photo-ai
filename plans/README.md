# Implementation Plans

Generated by the improve skill on 2026-09-04. Execute these plans in order. Read each plan fully before you start. Follow all STOP conditions.

## Execution order and status

| Plan | Title                                                 | Priority | Effort | Depends on | Status |
| ---- | ----------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Consolidate and merge the six open vibe pull requests | P1       | M      | —          | TODO   |
| 002  | Add four distinct Halloween portrait vibes            | P1       | M      | 001        | TODO   |

Status values: TODO, IN PROGRESS, DONE, BLOCKED, or REJECTED.

## Dependency notes

- Plan 001 must finish first. It changes the same theme, variation, discovery, homepage, asset, and test files that Plan 002 must change.
- Plan 002 must start from the updated `main` branch after the integration pull request from Plan 001 is merged.
- The current full test suite passes with 61 tests. Lint passes. The repository-wide format check already fails on seven unrelated files. Use a scoped Prettier check for files changed by these plans, and do not expand this work into a formatting cleanup.

## Decisions already made

- Do not merge the six open pull requests one at a time. Build one integration branch and resolve the shared-file conflicts once.
- Keep the newer PR #39 definition of `butter-yellow-picnic`. Do not keep the older PR #35 definition as a second entry.
- Keep all six original pull-request histories in the integration branch. Merge the integration pull request with a merge commit, not a squash commit.
- Add the requested Halloween concepts as four shoot vibes, not as greeting-card themes. This keeps optional card text out of the feature and makes all four available in the normal multi-vibe shoot flow.

## Findings considered and rejected

- Merge PR #33 and then merge PR #34 as separate GitHub pull requests: rejected because PR #34 already contains the full PR #33 commit. The integration branch can preserve both histories without repeated conflict work.
- Merge PRs #35 and #39 unchanged: rejected because both add the same `butter-yellow-picnic` theme ID and discovery slug.
- Reuse the existing `card-halloween` theme for the four requested concepts: rejected because it is one greeting-card layout and does not cover the four distinct art directions.
- Reuse `Vintage Pumpkin Patch Postcard` from PR #40 as `Vintage Costume Studio`: rejected because the existing proposal is a pumpkin-patch photograph, while the request is a painted 1920s–1950s costume-studio look.
